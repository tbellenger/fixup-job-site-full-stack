{{!--initiate page template for jobs categories--}}
<div class="hero-body">
<div class="container">
<div class="grid-container">
 {{!--group the job category into each column--}}
    {{#each jobs as |job|}}
   <div class="grid-item">{{>job-info job}}</div>
    {{/each}}
   </div> 
</div>
</div>